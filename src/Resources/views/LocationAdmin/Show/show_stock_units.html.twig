{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}


{% block field %}

    <div class="blast-show-collection" data-field-name="{{ field_description.name }}">

        <div class="box box-primary">
            <div class="box-header">
                <h4 class="box-title">{{ 'sil.stock.location.show.group.stockunits'|trans }}</h4>
            </div>

            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>{{ 'sil.stock.stockunit.show.label.created_at'|trans }}</th>
                        <th>{{ 'sil.stock.stockunit.show.label.code'|trans }}</th>
                        <th>{{ 'sil.stock.stockunit.show.label.stock_item'|trans }}</th>
                        <th>{{ 'sil.stock.stockunit.show.label.batch'|trans }}</th>
                        <th>{{ 'sil.stock.stockunit.show.label.qty'|trans }}</th>
                    </tr>   
                </thead>
                <tbody>
                   
                    {% for unit in object.getStockUnits() %}
                        <tr class="sonata-ba-view-container">
                            <td>{{ unit.createdAt|date('d/m/Y H:i')}}</td>
                            <td>{{unit.code }}</td>
                            <td>{{unit.stockItem.name }}</td>
                            <td>{{unit.batch}}</td>
                            <td>{{ unit.qty}}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

    </div>
{% endblock %}