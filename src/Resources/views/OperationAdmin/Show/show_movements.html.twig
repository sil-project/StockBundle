{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% set movement_admin = field_description.admin.show.elements.movements.associationadmin %}
{% set fields = movement_admin.show.elements %}


{% block field %}

    <div class="blast-show-collection" data-field-name="{{ field_description.name }}">


        <div class="box box-primary">
            <div class="box-header">
                <h4 class="box-title">{{ 'show_group_movements'|trans }}</h4>
            </div>

            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>{{ fields['code'].options.label|trans }}</th>
                        <th>{{ 'librinfo.label.stock_item'|trans }}</th>
                        <th>{{ 'librinfo.label.location_from'|trans }}</th>
                        <th>{{ 'librinfo.label.location_to'|trans }}</th>
                        <th>{{ 'librinfo.label.qty'|trans }}</th>
                        <th>{{ 'librinfo.label.state'|trans }}</th>
                    </tr>   
                </thead>
                <tbody>

                    {% for movement in value %}
                        <tr class="sonata-ba-view-container">
                            <td>{{ movement.code}}</td>
                            <td>{{ movement.stockItem.name }}</td>
                            <td>{{ movement.srcLocation.name }}</td>
                            <td>{{ movement.destLocation.name }}</td>
                            <td>{{ movement.qty }}</td>
                            <td>{% include('SilStockBundle:OperationAdmin/Show:_operation_state.html.twig') with {'value': movement.state} %}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

    </div>
{% endblock %}